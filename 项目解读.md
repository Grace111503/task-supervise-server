src/pages/: 你的主战场。所有业务页面都放在这里。模板支持约定式路由，你在这个目录下新建.vue文件，路由就会自动生成，非常方便。

src/api/: 后端对话的地方。在这里封装所有与后端交互的接口函数。模板已经基于 @uni-helper/uni-network 封装好了 http 实例，你直接用它来定义接口即可，示例可以参考 src/api/user.ts。

src/store/: 管理全局状态。这里存放 Pinia 状态管理文件，很适合存放用户信息、任务列表这类跨组件共享的数据。模板已经配置好了持久化插件，数据会自动保存在本地。

src/components/: 复用组件仓库。把你的业务组件，比如“任务卡片”、“进度反馈表单”等放在这里，它们会自动全局注册，在页面中可以直接使用